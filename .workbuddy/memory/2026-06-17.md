# 2026-06-17 开发日志

## OIDC 用户登录模块
- **新增 `src/js/services/auth.js`**: Authing OIDC 认证服务
  - 授权码 + PKCE 流程（S256）
  - Token 管理：access_token / id_token / refresh_token 本地持久化
  - 自动刷新（80% 过期时间触发）
  - OIDC 端点: `https://weinvest.authing.cn/oidc`
  - Client ID 配置弹窗（首次登录时引导）
  - 用户信息获取（/oidc/me）
  - 登出（end_session_endpoint）

- **前端 UI 更新**:
  - Top bar 加入登录按钮 / 用户头像 / 用户名 / 下拉菜单
  - Sidebar footer: P2P 状态区可点击，加入"在线用户"按钮
  - 在线用户面板（overlay 弹窗，显示用户列表 + 在线状态）
  - 设置页新增 Authing 认证配置卡片（App ID / OIDC 端点 / 登录状态）
  - CSS: auth-area / auth-modal / online-users-panel 等完整样式

- **`app.js` 集成**:
  - 启动时初始化 AuthService（优先处理 OIDC 回调）
  - Auth state change 更新 UI 和 Gun.js 用户档案
  - 在线用户面板打开/关闭/加载逻辑

- **`gun-sync.js` 更新**:
  - 新增 `setUserProfile()` 方法：登录后广播用户在线状态
  - 新增 `getAuthUser()` 方法
  - Chat 消息使用 Authing 登录用户名

## Authing 后端代理
- **新增 `server/services/auth_service.py`**:
  - Authing Management API 代理（获取应用活跃用户）
  - 管理 Token 缓存与自动刷新
  - Mock 数据 fallback

- **新增 `server/routers/auth.py`**:
  - `GET /api/auth/online-users` — 获取在线用户列表
  - `GET /api/auth/me` — 获取当前用户信息
  - `POST /api/auth/logout` — 登出

- **`server/config.py` 更新**: 加入 Authing OIDC / Management API 配置项
- **`server/main.py` 更新**: 注册 auth 路由

## 配置项
- OIDC: `EBI_AUTHING_OIDC_ISSUER` / `EBI_AUTHING_DOMAIN` / `EBI_AUTHING_APP_ID` / `EBI_AUTHING_APP_SECRET` / `EBI_AUTHING_USERPOOL_ID`
- 前端: localStorage `ebi_auth_client_id`
